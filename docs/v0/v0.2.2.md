# v0.2.2 — Root Cleanup

**Goal:** Make the repository root clean, obvious, and useful before backend architecture changes.

**Status:** Complete

---

## Scope

This branch is root hygiene only:
- one tool-neutral agent instructions file
- keep `.cursor` because it contains custom MCP configuration
- concise developer README
- remove stale root `.env.example`
- no backend or frontend source moves

---

## Decisions

| Area | Decision |
|---|---|
| Agent entrypoint | Use root `AGENTS.md` as the single tool-neutral instructions file |
| Duplicate agent docs | Remove `backend/AGENTS.md` |
| Cursor files | Keep `.cursor` for custom MCP configuration |
| README | Add a concise root developer README |
| Root env example | Remove root `.env.example`; env examples belong with each app |

---

## Out of Scope

- Backend architecture restructure
- Frontend architecture restructure
- Runtime behavior changes
- Provider cleanup
- API route renames

Those belong in later branches:
- `fix/v0.2.3/backend-architecture`
- `fix/v0.2.4/frontend-architecture`
